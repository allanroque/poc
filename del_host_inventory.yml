---
- name: Remover host em um inventario de self-healing
  hosts: localhost
  gather_facts: False
  tasks:

  - name: Remover host
    ansible.controller.host:
      name: "{{ ansible_hosts }}"
      description: "adicionado para self-healing"
      inventory: inventory_selfhealing
      state: absent
